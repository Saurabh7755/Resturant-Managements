@model IEnumerable<Resturant.Web.Models.Customer>

@{
    ViewData["Title"] = "Index";
}
@*<br />
<br />
<br />
<h1 style="text-align:center">Customers</h1>


<table class="table table-striped table-hover">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CustomerName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Phone)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CustomerName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Phone)
            </td>
            <td>
                    <a asp-action="Edit" asp-route-id="@item.CustomerID" button type="button" class="btn btn-primary">Edit</a> |
                    <a asp-action="Details" asp-route-id="@item.CustomerID" button type="button" class="btn btn-warning">Details</a> |
                    <a asp-action="Delete" asp-route-id="@item.CustomerID" button type="button" class="btn btn-danger">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>*@

<style>
    body {
        background-image: url(/lib/images/backhome1.jfif);
        background-repeat: no-repeat;
        background-attachment: fixed;
        background-size: 100% 100%;
    }
</style>
<br />
<br />
<br />
<br />
<p>
    <a asp-action="Create">Create New</a>
</p>
<div class="card-columns">
    @foreach (var item in Model)
    {
        <div class="card">
            <div class="card-header">
                <b>Customer NO:</b> @item.CustomerID
            </div>
            <div class="card-body">

                <b><b> @Html.DisplayNameFor(modelItem => item.CustomerName) : @item.CustomerName</b></b>
                <br />

                <b><b>@Html.DisplayNameFor(modelItem =>item.Phone) : @item.Phone</b></b>
                <br />

              

              


            </div>
            <div class="card-footer">
                <a asp-action="Edit" asp-route-id="@item.CustomerID" button type="button" class="btn btn-primary">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CustomerID" button type="button" class="btn btn-warning">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CustomerID" button type="button" class="btn btn-danger">Delete</a>
            </div>
        </div>
    }
</div>

